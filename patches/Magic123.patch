diff --git a/freqencoder/__init__.py b/freqencoder/__init__.py
index 69ec49c..980c195 100644
--- a/freqencoder/__init__.py
+++ b/freqencoder/__init__.py
@@ -1 +1,11 @@
-from .freq import FreqEncoder
\ No newline at end of file
+import os
+import sys
+
+_build_root = os.path.join(os.path.dirname(os.path.abspath(__file__)), "build")
+if os.path.isdir(_build_root):
+    for _entry in sorted(os.listdir(_build_root), reverse=True):
+        _path = os.path.join(_build_root, _entry)
+        if os.path.isdir(_path) and _path not in sys.path:
+            sys.path.insert(0, _path)
+
+from .freq import FreqEncoder
diff --git a/freqencoder/backend.py b/freqencoder/backend.py
index fa0e820..7ccd6da 100644
--- a/freqencoder/backend.py
+++ b/freqencoder/backend.py
@@ -4,13 +4,13 @@ from torch.utils.cpp_extension import load
 _src_path = os.path.dirname(os.path.abspath(__file__))
 
 nvcc_flags = [
-    '-O3', '-std=c++14',
+    '-O3', '-std=c++17',
     '-U__CUDA_NO_HALF_OPERATORS__', '-U__CUDA_NO_HALF_CONVERSIONS__', '-U__CUDA_NO_HALF2_OPERATORS__',
     '-use_fast_math'
 ]
 
 if os.name == "posix":
-    c_flags = ['-O3', '-std=c++14']
+    c_flags = ['-O3', '-std=c++17']
 elif os.name == "nt":
     c_flags = ['/O2', '/std:c++17']
 
@@ -39,4 +39,4 @@ _backend = load(name='_freqencoder',
                 ]],
                 )
 
-__all__ = ['_backend']
\ No newline at end of file
+__all__ = ['_backend']
diff --git a/freqencoder/setup.py b/freqencoder/setup.py
index ea64112..4a1fd52 100644
--- a/freqencoder/setup.py
+++ b/freqencoder/setup.py
@@ -5,13 +5,13 @@ from torch.utils.cpp_extension import BuildExtension, CUDAExtension
 _src_path = os.path.dirname(os.path.abspath(__file__))
 
 nvcc_flags = [
-    '-O3', '-std=c++14',
+    '-O3', '-std=c++17',
     '-U__CUDA_NO_HALF_OPERATORS__', '-U__CUDA_NO_HALF_CONVERSIONS__', '-U__CUDA_NO_HALF2_OPERATORS__',
     '-use_fast_math'
 ]
 
 if os.name == "posix":
-    c_flags = ['-O3', '-std=c++14']
+    c_flags = ['-O3', '-std=c++17']
 elif os.name == "nt":
     c_flags = ['/O2', '/std:c++17']
 
diff --git a/gridencoder/__init__.py b/gridencoder/__init__.py
index f1476ce..bd20291 100644
--- a/gridencoder/__init__.py
+++ b/gridencoder/__init__.py
@@ -1 +1,11 @@
-from .grid import GridEncoder
\ No newline at end of file
+import os
+import sys
+
+_build_root = os.path.join(os.path.dirname(os.path.abspath(__file__)), "build")
+if os.path.isdir(_build_root):
+    for _entry in sorted(os.listdir(_build_root), reverse=True):
+        _path = os.path.join(_build_root, _entry)
+        if os.path.isdir(_path) and _path not in sys.path:
+            sys.path.insert(0, _path)
+
+from .grid import GridEncoder
diff --git a/gridencoder/backend.py b/gridencoder/backend.py
index b403f34..8686fbb 100644
--- a/gridencoder/backend.py
+++ b/gridencoder/backend.py
@@ -4,12 +4,12 @@ from torch.utils.cpp_extension import load
 _src_path = os.path.dirname(os.path.abspath(__file__))
 
 nvcc_flags = [
-    '-O3', '-std=c++14',
+    '-O3', '-std=c++17',
     '-U__CUDA_NO_HALF_OPERATORS__', '-U__CUDA_NO_HALF_CONVERSIONS__', '-U__CUDA_NO_HALF2_OPERATORS__',
 ]
 
 if os.name == "posix":
-    c_flags = ['-O3', '-std=c++14']
+    c_flags = ['-O3', '-std=c++17']
 elif os.name == "nt":
     c_flags = ['/O2', '/std:c++17']
 
@@ -37,4 +37,4 @@ _backend = load(name='_grid_encoder',
                 ]],
                 )
 
-__all__ = ['_backend']
\ No newline at end of file
+__all__ = ['_backend']
diff --git a/gridencoder/setup.py b/gridencoder/setup.py
index a91b0c1..f928080 100644
--- a/gridencoder/setup.py
+++ b/gridencoder/setup.py
@@ -5,12 +5,12 @@ from torch.utils.cpp_extension import BuildExtension, CUDAExtension
 _src_path = os.path.dirname(os.path.abspath(__file__))
 
 nvcc_flags = [
-    '-O3', '-std=c++14',
+    '-O3', '-std=c++17',
     '-U__CUDA_NO_HALF_OPERATORS__', '-U__CUDA_NO_HALF_CONVERSIONS__', '-U__CUDA_NO_HALF2_OPERATORS__',
 ]
 
 if os.name == "posix":
-    c_flags = ['-O3', '-std=c++14']
+    c_flags = ['-O3', '-std=c++17']
 elif os.name == "nt":
     c_flags = ['/O2', '/std:c++17']
 
diff --git a/guidance/sd_utils.py b/guidance/sd_utils.py
index 4bef064..e918296 100644
--- a/guidance/sd_utils.py
+++ b/guidance/sd_utils.py
@@ -148,7 +148,7 @@ class StableDiffusion(nn.Module):
 
         # Create model
         pipe = StableDiffusionPipeline.from_pretrained(
-            sd_path, torch_dtype=self.precision_t, local_files_only=False)
+            sd_path, torch_dtype=self.precision_t, local_files_only=True)
 
         if isfile('./unet_traced.pt'):
             # use jitted unet
@@ -189,7 +189,7 @@ class StableDiffusion(nn.Module):
             pipe.to(device)
 
         self.scheduler = DDIMScheduler.from_pretrained(
-            sd_path, subfolder="scheduler", torch_dtype=self.precision_t, local_files_only=False)
+            sd_path, subfolder="scheduler", torch_dtype=self.precision_t, local_files_only=True)
 
         self.num_train_timesteps = self.scheduler.config.num_train_timesteps
         self.min_step = int(self.num_train_timesteps * t_range[0])
diff --git a/main.py b/main.py
index 4e60676..bb6e866 100644
--- a/main.py
+++ b/main.py
@@ -538,8 +538,9 @@ if __name__ == '__main__':
             if opt.save_mesh:
                 try:
                     trainer.save_mesh()
-                except:
-                    pass
+                except Exception:
+                    import traceback
+                    traceback.print_exc()
     else:
         train_loader = NeRFDataset(
             opt, device=device, type='train', H=opt.h, W=opt.w, size=opt.dataset_size_train * opt.batch_size).dataloader()
@@ -627,4 +628,4 @@ if __name__ == '__main__':
                 try:
                     trainer.save_mesh()
                 except:
-                    pass
\ No newline at end of file
+                    pass
diff --git a/meshutils.py b/meshutils.py
index 4d1c53d..3455ce9 100644
--- a/meshutils.py
+++ b/meshutils.py
@@ -1,5 +1,9 @@
 import numpy as np
-import pymeshlab as pml
+
+try:
+    import pymeshlab as pml
+except ModuleNotFoundError:
+    pml = None
 
 def poisson_mesh_reconstruction(points, normals=None):
     # points/normals: [N, 3] np.ndarray
@@ -42,6 +46,10 @@ def decimate_mesh(verts, faces, target, backend='pymeshlab', remesh=False, optim
     _ori_vert_shape = verts.shape
     _ori_face_shape = faces.shape
 
+    if backend == 'pymeshlab' and pml is None:
+        print('[WARN] pymeshlab is not installed; skipping mesh decimation.')
+        return verts, faces
+
     if backend == 'pyfqmr':
         import pyfqmr
         solver = pyfqmr.Simplify()
@@ -79,6 +87,24 @@ def clean_mesh(verts, faces, v_pct=1, min_f=8, min_d=5, repair=True, remesh=True
     _ori_vert_shape = verts.shape
     _ori_face_shape = faces.shape
 
+    if pml is None:
+        valid = (
+            (faces[:, 0] != faces[:, 1])
+            & (faces[:, 0] != faces[:, 2])
+            & (faces[:, 1] != faces[:, 2])
+        )
+        faces = faces[valid]
+        used = np.unique(faces.reshape(-1))
+        remap = np.full(len(verts), -1, dtype=np.int64)
+        remap[used] = np.arange(len(used))
+        verts = verts[used]
+        faces = remap[faces]
+        print(
+            f'[WARN] pymeshlab is not installed; using minimal mesh cleanup: '
+            f'{_ori_vert_shape} --> {verts.shape}, {_ori_face_shape} --> {faces.shape}'
+        )
+        return verts, faces
+
     m = pml.Mesh(verts, faces)
     ms = pml.MeshSet()
     ms.add_mesh(m, 'mesh') # will copy!
@@ -114,4 +140,4 @@ def clean_mesh(verts, faces, v_pct=1, min_f=8, min_d=5, repair=True, remesh=True
 
     print(f'[INFO] mesh cleaning: {_ori_vert_shape} --> {verts.shape}, {_ori_face_shape} --> {faces.shape}')
 
-    return verts, faces    
\ No newline at end of file
+    return verts, faces    
diff --git a/raymarching/__init__.py b/raymarching/__init__.py
index 26d3cc6..c8e3f1b 100644
--- a/raymarching/__init__.py
+++ b/raymarching/__init__.py
@@ -1 +1,11 @@
-from .raymarching import *
\ No newline at end of file
+import os
+import sys
+
+_build_root = os.path.join(os.path.dirname(os.path.abspath(__file__)), "build")
+if os.path.isdir(_build_root):
+    for _entry in sorted(os.listdir(_build_root), reverse=True):
+        _path = os.path.join(_build_root, _entry)
+        if os.path.isdir(_path) and _path not in sys.path:
+            sys.path.insert(0, _path)
+
+from .raymarching import *
diff --git a/raymarching/backend.py b/raymarching/backend.py
index 7cc0d76..55a926b 100644
--- a/raymarching/backend.py
+++ b/raymarching/backend.py
@@ -4,12 +4,12 @@ from torch.utils.cpp_extension import load
 _src_path = os.path.dirname(os.path.abspath(__file__))
 
 nvcc_flags = [
-    '-O3', '-std=c++14',
+    '-O3', '-std=c++17',
     '-U__CUDA_NO_HALF_OPERATORS__', '-U__CUDA_NO_HALF_CONVERSIONS__', '-U__CUDA_NO_HALF2_OPERATORS__',
 ]
 
 if os.name == "posix":
-    c_flags = ['-O3', '-std=c++14']
+    c_flags = ['-O3', '-std=c++17']
 elif os.name == "nt":
     c_flags = ['/O2', '/std:c++17']
 
@@ -38,4 +38,4 @@ _backend = load(name='_raymarching',
                 ]],
                 )
 
-__all__ = ['_backend']
\ No newline at end of file
+__all__ = ['_backend']
diff --git a/raymarching/setup.py b/raymarching/setup.py
index 4d32fa7..4f6ceae 100644
--- a/raymarching/setup.py
+++ b/raymarching/setup.py
@@ -5,12 +5,12 @@ from torch.utils.cpp_extension import BuildExtension, CUDAExtension
 _src_path = os.path.dirname(os.path.abspath(__file__))
 
 nvcc_flags = [
-    '-O3', '-std=c++14',
+    '-O3', '-std=c++17',
     '-U__CUDA_NO_HALF_OPERATORS__', '-U__CUDA_NO_HALF_CONVERSIONS__', '-U__CUDA_NO_HALF2_OPERATORS__',
 ]
 
 if os.name == "posix":
-    c_flags = ['-O3', '-std=c++14']
+    c_flags = ['-O3', '-std=c++17']
 elif os.name == "nt":
     c_flags = ['/O2', '/std:c++17']
 
diff --git a/shencoder/__init__.py b/shencoder/__init__.py
index 2b55c96..5631698 100644
--- a/shencoder/__init__.py
+++ b/shencoder/__init__.py
@@ -1 +1,11 @@
-from .sphere_harmonics import SHEncoder
\ No newline at end of file
+import os
+import sys
+
+_build_root = os.path.join(os.path.dirname(os.path.abspath(__file__)), "build")
+if os.path.isdir(_build_root):
+    for _entry in sorted(os.listdir(_build_root), reverse=True):
+        _path = os.path.join(_build_root, _entry)
+        if os.path.isdir(_path) and _path not in sys.path:
+            sys.path.insert(0, _path)
+
+from .sphere_harmonics import SHEncoder
diff --git a/shencoder/backend.py b/shencoder/backend.py
index 4971d5e..81fd9d1 100644
--- a/shencoder/backend.py
+++ b/shencoder/backend.py
@@ -4,12 +4,12 @@ from torch.utils.cpp_extension import load
 _src_path = os.path.dirname(os.path.abspath(__file__))
 
 nvcc_flags = [
-    '-O3', '-std=c++14',
+    '-O3', '-std=c++17',
     '-U__CUDA_NO_HALF_OPERATORS__', '-U__CUDA_NO_HALF_CONVERSIONS__', '-U__CUDA_NO_HALF2_OPERATORS__',
 ]
 
 if os.name == "posix":
-    c_flags = ['-O3', '-std=c++14']
+    c_flags = ['-O3', '-std=c++17']
 elif os.name == "nt":
     c_flags = ['/O2', '/std:c++17']
 
@@ -38,4 +38,4 @@ _backend = load(name='_sh_encoder',
                 ]],
                 )
 
-__all__ = ['_backend']
\ No newline at end of file
+__all__ = ['_backend']
diff --git a/shencoder/setup.py b/shencoder/setup.py
index 4633ebd..fe788c3 100644
--- a/shencoder/setup.py
+++ b/shencoder/setup.py
@@ -5,12 +5,12 @@ from torch.utils.cpp_extension import BuildExtension, CUDAExtension
 _src_path = os.path.dirname(os.path.abspath(__file__))
 
 nvcc_flags = [
-    '-O3', '-std=c++14',
+    '-O3', '-std=c++17',
     '-U__CUDA_NO_HALF_OPERATORS__', '-U__CUDA_NO_HALF_CONVERSIONS__', '-U__CUDA_NO_HALF2_OPERATORS__',
 ]
 
 if os.name == "posix":
-    c_flags = ['-O3', '-std=c++14']
+    c_flags = ['-O3', '-std=c++17']
 elif os.name == "nt":
     c_flags = ['/O2', '/std:c++17']
 
